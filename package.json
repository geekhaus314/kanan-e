{
  "name": "kananos",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean",
    "db:generate": "pnpm --filter @kananos/database db:generate",
    "db:push": "pnpm --filter @kananos/database db:push",
    "db:seed": "pnpm --filter @kananos/database db:seed"
  },
  "dependencies": {
    "next": "^15.3.0"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=22.0.0"
  }
}
